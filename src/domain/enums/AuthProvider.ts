/**
 * Enum representing the authentication provider type for the user.
 *
 * @enum
 */
export enum AuthProvider {
  /**
   * Representing Google oAuth user
   */
  GOOGLE = 'GOOGLE',

  /**
   * Representing Local auth user
   */
  LOCAL = 'LOCAL',
}